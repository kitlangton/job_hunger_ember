<h3>New Lead</h3>

<form>
  <div class="form-group">
    <label>Name</label>
    {{input value=name class="form-control"}}
  </div>

  <select value=companyId>
    <option value=''></option>
    {{#each model as |company|}}
      <option value={{company.id}}>
        {{ company.name }}
      </option>
    {{/each}}
  </select>

  <button class="btn-success" {{action "createLead" name companyId}}>Create Lead</button>
</form>
