<h2 id="title">Job Hunger</h2>

{{#if session.isAuthenticated}}
<button class="submit btn btn-danger" {{action "invalidateSession"}}>Log Out</button>
<span class="label label-info">Score: {{ sessionAccount.currentUser.score }}</span>
{{else}}
{{link-to "Log In" "login"}}
{{/if}}
<br/>


<div class="container">
  {{outlet}}
</div>
