<div class="container">
  {{nav-bar invalidateSession=(action 'invalidateSession')}}
  {{#if session.isAuthenticated}}
    <span class="label label&#45;info">Score: {{ sessionAccount.currentUser.score }}</span>
  {{/if}}
  {{outlet}}
</div>

<div class="preload">
  <div class="nav-bar">
  <div class="logo ooze-open"></div>
  <div class="logo ooze"></div>
  </div>
</div>
